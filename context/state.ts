import { InitialState } from './stateTypes';

const initialState: InitialState = {
  error: false,
  headerVisible: true,
  footerVisible: false,
  loaderVisible: true,
};

export default initialState;
